{
  "compilerOptions": {
    "target": "es6",                      // Визначає версію ECMAScript для компільованого коду
    "module": "commonjs",                 // Визначає формат модулів
    "strict": true,                       // Включає всі строгі перевірки типів
    "esModuleInterop": true,              // Дозволяє імпорт модулів CommonJS в стилі ES
    "skipLibCheck": true,                 // Пропускає перевірку типів у бібліотеках
    "outDir": "./dist",                   // Визначає директорію для скомпільованих файлів
    "rootDir": "./src",                   // Визначає кореневу директорію для вихідних файлів
    "baseUrl": "./",                      // Встановлює базовий шлях для абсолютних модулів
    "paths": {
      "*": ["node_modules/*"]
    }
  },
  "include": [
    "src/**/*.ts"                         // Включає всі TypeScript файли в директорії src
  ],
  "exclude": [
    "node_modules"                        // Вилучає директорію node_modules
  ]
}
